# Certificates Directory

This folder is created for you to paste all your certificate images (e.g., `.png`, `.jpg`, `.jpeg`, `.webp`).

## How to use:
1. Paste your certificate images into this folder.
2. Update the certificates in `index.html` under the `#certificates` section by referencing the path `assets/images/certificates/your-image-name.png`.
